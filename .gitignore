# --- dependencies / build output ---
node_modules
dist
dist-ssr
coverage
*.local

# --- editor / OS ---
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# --- logs ---
*.log
npm-debug.log*
pnpm-debug.log*

# ============================================================
# SECURITY / EXAM-INTEGRITY — NEVER COMMIT (HARD RULES §13)
# These are gitignored DEFENSIVELY; no build/import code path reads them.
# Only the hash-only derivatives in ci/ are tracked.
# ============================================================
reference-scratch/
*.pdf
**/machine-list*
denylist.local.*
.env*

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
