# Cephalo v2 — The Arsenal / Deterministic-Engines Upgrade (README-v2)

Two finalized addendum prompts that the **same** self-driving build agent folds into the **already-running** Cephalo build (`/home/vlad/bigOscpPrep`) and `/loop`. They are **purely additive**: frozen `src/types/{content,engine,components,tokens}.ts` + `schema-parity.test.ts` stay **byte-identical**. New shapes go in new sibling modules; new surfaces are new routes; new design tokens are new `--cph-` KEYS only. Where the two prompts disagree with anything, the embedded **v2 ADDITIVE CONTRACT wins** (it is the single source of truth and is embedded verbatim in both).

## What each addendum owns

**1. `_prompts/CODE-PROMPT-v2-arsenal.md` — the engineering authority (KEYS + behavior).**
Owns the *what-exists-and-how-it-behaves*: the new type modules (`src/types/{arsenal,advisor,ask,cve,decision,nmap}.ts`, `components.v2.ts`, `tokens.v2.ts`, `bundle.v2.ts`), the five new pure deterministic engines (intent resolve, advisor parse, CVE lookup, decision walk, nmap parse/route), the new routes (`/ask`, `/advisor`, `/cve`, `/arsenal`, `/$os/decision/$mapId`), the new Zustand stores, the `CephaloV2Bundle` + per-dataset JSON artifacts emitted by `scripts/build-v2.ts`, the Zod parity (`schema.v2.ts` + `schema-parity.v2.test.ts`), the new CI gates (G15–G21 + `tool-provenance`, `advisor-coverage`, `intent-resolves`, `decision-reachability`, `nmap-route-coverage`, extended token-KEY parity, determinism snapshots), and the new LOOP checklist blocks. **Declares** every `--cph-` KEY in `TOKEN_KEYS_V2`; supplies **no** values.

**2. `_prompts/DESIGN-PROMPT-v2-arsenal.md` — the visual authority (VALUES).**
Owns the *how-it-looks*: "The Summoning Chamber" art direction extending v1's Abyssal Instrument, the big anatomically-richer hero **Octo** on `/ask` (`AskOctopusHero` composing the frozen `OctopusMascot` — never a new mascot state, never `navigateTo`), and concrete VALUES for all **38 new `--cph-` keys** (ask 9, advisor 6, decision 9, tool 3, cve 4, nmap 5, shared 2). Binds to the new prop modules by import; never renames a prop, redefines an enum value, or invents a token key. Enforces the load-bearing aesthetic: **explainable retrieval, not oracle** — alive and instant, yet visibly non-AI (no thinking-spinner-that-implies-a-model, no chat bubbles, no "generating…").

## The non-negotiable shared spine (in both)
ZERO AI (pure deterministic functions over curated data + regex + the existing offline MiniSearch; no `Math.random`, no ambient clock, `id` as final sort tiebreak, byte-reproducible) · EXAM-LEGAL FRAMING (retrieves your own notes + fills variables you set; never scans/exploits/automates; persistent banner + per-feature explain-why chips + `[UNVERIFIED]` badges) · OFFLINE/LOCAL-FIRST (no runtime net, pasted enum/CVE/nmap inputs are session-only, theme switch proposed-not-applied, autofill never overwrites a user-set var) · NO BUNDLED BINARY (link official release PAGES via frozen `Reference`; every `ToolBinary` carries the literal `fetchNote`; `searchsploit` is the only copy-to-clipboard escape hatch) · ANTI-FABRICATION (uncertain ⇒ literal `[UNVERIFIED]` + non-empty `references[]` + human reason; RFC5737/RFC3849 defanged placeholders) · ADDITIVE-ONLY SEAM (reuse the ONE `Severity`/`Confidence`/`OS`/`Shell`/`CredMode`/`Phase`/`OctoState`; new `tool.*`/`sig.*`/`rule.*`/`alias.*`/`phrase.*`/`cve.*`/`dec.*`/`route.*`/`nmap.*` id namespaces FK into frozen ids but are never frozen content ids).

A v2 leaf is `☑` only when it is content-complete **and** its CI gate is green — exactly the rule the existing loop already runs.